plate = input("Plate: ")
for char in plate:
    print(char, char.isalpha())
if plate[0].isalpha() && plate[1].isalpha:
    if plate.size() > 2 && plate.size() < 6:
        for char in plate:
            if char == '.' || char == ' '
            else:
                break
            if
